# Core API + baseline ML (CPU-friendly; no PyTorch)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
python-multipart>=0.0.6

numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
nltk>=3.8.0
joblib>=1.3.0

# Optional: spaCy for alternative preprocessing (not required by default)
# pip install spacy && python -m spacy download en_core_web_sm
